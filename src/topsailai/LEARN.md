When user provides negative or corrective feedback, treat it as high-signal design constraints rather than just a code-location fix. In the auto session_name task, the user first rejected placing logic in session_manager/sql.py, then had to explicitly specify get_llm_chat(session_id="", need_stdout=False). Lessons: (1) proactively ask where business logic belongs when user rejects a layer; (2) for LLM side effects that should not pollute session history or stdout, default to session_id="" and need_stdout=False without being told; (3) negative feedback often reveals unstated architectural rules—extract and confirm them immediately.