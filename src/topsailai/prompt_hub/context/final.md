# Final Answer Requirement

When you are ready to output `final_answer`, make sure the `final_answer` step contains the complete reasoning and conclusion. Do not split the reasoning into a separate preceding `thought` or `inquiry` step; include the reasoning inside the `final_answer` step itself.
